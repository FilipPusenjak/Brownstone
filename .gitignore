node_modules/
.pnp
.pnp.js

.next/
out/
build/
dist/
next-env.d.ts

.env
.env.local
.env*.local

# Generated Prisma client
src/generated/

# Local drivers: uploaded files and caught mail. Both are dev-only artifacts.
# Anchored to the repo root — an unanchored "storage/" also matches
# src/lib/storage/, the real storage-driver module, and silently excludes it
# from every commit. That happened here; see git history.
/storage/
.mail/

coverage/
playwright-report/
test-results/
.playwright/

*.tsbuildinfo
.DS_Store
*.log
npm-debug.log*

/src/generated/prisma
